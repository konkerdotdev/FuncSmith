<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" >
  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/default.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/base16/gruvbox-dark-pale.min.css">
  <link rel="stylesheet" href="/styles/style.css">
  <meta name="generator" content="funcsmith">
</head>
<body>
  <div class="container">
    <header>
      <nav>
        {{ siteName }} |
        <a href="/">Home</a> |
        <a href="/about">About</a>
      </nav>
      <h1>{{ title }}<time class="float-right">{{ formattedDate date }}</time></h1>
    </header>

    <nav>
    {{#if collection.index}}
      <span> ^ </span><a href="{{collection.index.link}}">{{collection.index.title}}</a>
      <span class="sep">|</span>
    {{/if}}
    {{#if collection.previous}}
      <span> &lt; </span><a href="{{collection.previous.link}}">{{collection.previous.title}}</a>
      <span class="sep">|</span>
    {{/if}}
    {{#if collection.next}}
      <a href="{{collection.next.link}}">{{collection.next.title}}</a><span> &gt; </span>
    {{/if}}
    </nav>

    <main>
      {{{ contents }}}
    </main>

    {{> footer.hbs }}
  </div>
</body>
</html>
